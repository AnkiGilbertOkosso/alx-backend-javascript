/**
 * Displays a message on the STDOUT.
 * @param {String} message The message to display.
 * @documentary This function logs a message to the standard output.
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
